import { TTakeDetailId } from "src/core/typings/app";
import { TDetails } from "src/core/typings/details";

export type TDetailsProps = {
    details: TDetails | undefined
    takeDetailId: TTakeDetailId
}